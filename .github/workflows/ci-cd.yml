name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to DockerHub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and Push Docker Image
      uses: docker/build-push-action@v4
      with:
        context: .
        file: Dockerfile
        push: true
        tags: ${{ secrets.DOCKER_USERNAME }}/lab1-app:latest

    - name: Install Railway CLI
      run: |
        curl -sSL https://railway.app/install.sh | sh
        echo "export PATH=$HOME/.railway/bin:$PATH" >> $GITHUB_ENV

    - name: Set Railway Token
      run: echo "RAILWAY_TOKEN=${{ secrets.RAILWAY_TOKEN }}" >> $GITHUB_ENV

    - name: Deploy to Railway
      run: |
        railway up --project-id ${{ secrets.RAILWAY_PROJECT_ID }} --service docker --image ${{ secrets.DOCKER_USERNAME }}/lab1-app:latest
